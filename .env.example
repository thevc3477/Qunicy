# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ====================================================================
# IMPORTANT: Spotify API Credentials
# ====================================================================
# These credentials should NOT be added to this .env file!
# They must be configured as Supabase Edge Function secrets.
#
# Setup Instructions:
# 1. Get Spotify credentials from https://developer.spotify.com/dashboard
#    - Create an app (NO redirect URIs needed - we use Client Credentials flow)
#    - Copy Client ID and Client Secret
#
# 2. Add secrets to Supabase Edge Functions:
#    Using Supabase CLI:
#      supabase secrets set SPOTIFY_CLIENT_ID=your_client_id
#      supabase secrets set SPOTIFY_CLIENT_SECRET=your_client_secret
#
#    Or via Supabase Dashboard:
#      Project Settings > Edge Functions > Secrets
#
# 3. Deploy the Edge Function:
#      supabase functions deploy spotify-search
#
# The secrets are stored securely on the server and never exposed to the client.
# ====================================================================
